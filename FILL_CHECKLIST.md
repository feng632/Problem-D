# 论文数据回填清单(编程手 ↔ 论文手交接)

> 本文档是"编程手跑数据 → 论文手回填"的交接单。
> **占位符约定**:论文中 `\textbf{(待回填:……)}` 与 `(代码手填入)` 是给编程手留的数值空位,
> 求解脚本跑完后由**论文手**逐个替换;替换时**不要删除占位符周边文字**,只换数字。
> 回填完成后按末尾自检清单,再 `make pdf`。
> **行号注意**:正文段落重排后行号会偏移,下列"论文落点"行号仅供快速定位,
> 精确位置以公式标签/表格 label 为准。

---

## 0. 总览

| 脚本(编程手创建) | 输出内容 | 论文落点 |
|---|---|---|
| `code/src/solve_q1.py` | | |
| `code/src/solve_q2.py` | | |
| `code/src/solve_q3.py` | | |
| `code/src/solve_q4.py` | | |
| `code/src/solve_q5.py` | | |

**数据**:发题后在 `data/` 与 `code/data/` 就位,读法写入 `data/README.md`。
**公共约定**:留出法划分、固定随机种子(写入脚本注释)、输入特征标准化;所有脚本输出保留 4 位小数(百分比保留 2 位)。

---

## 1. solve_q1.py —— 问题一(对应 5.1 节)

- [ ] 建模手在 `04-models.tex` 写下模型公式后,把本节的"输出内容"清单补全
- [ ] 论文落点:对应公式/表格 label

## 2. solve_q2.py —— 问题二(6.1 节精度检验)

- [ ] 同上,发题后按实际模型补全

## 3. solve_q3.py —— 问题三(5.3 节优化结果)

- [ ] 同上

## 4. solve_q4.py —— 问题四(5.4 节)

- [ ] 同上

## 5. solve_q5.py —— 问题五(5.5 节 + 6.2 节)

- [ ] 同上

---

## 6. 论文手新增呈现物汇总(编程手跑完数据后要做)

| 新增内容 | 位置 |
|---|---|
| 各问结果表(按 04-models.tex 中 `% 待新增` 注释) | `04-models.tex` 各小节末 |
| 摘要(每问一段结果数字) | `main.tex` 摘要(仍是"……"占位) |
| 参考文献补真实文献 | `08-references.tex`(两条示例占位) |
| 附录核心代码 | `09-appendix.tex` |
| 图:各问 fig 脚本 → `make fig` | `code/src/*_fig.py` |

---

## 7. 回填完成后的自检清单(论文手)

1. `grep -rn "待回填\|代码手填入" paper/sections/` 应为空(注释里的除外);`main.tex` 摘要中的"……"占位已全部替换
2. 各节结论句与结果表数字一致(如最敏感参数结论与敏感性表排序一致)
3. 新增结果表满足建模手写的可行域/约束定义
4. `make pdf` 通过;`make pack` 后核对 submission/ 下 zip、md5、重命名 PDF
