% =====================================================================
%  02 模型假设(建模手)
%  写出建模时做的合理假设,每条假设在建模中会被用到,
%  尽量每条都能对应到 04 模型的建立与求解里的某个式子/符号。
%
%  符号说明表:由队长(Claude)统一维护,全队以此为准。
% =====================================================================
\section{模型假设}

% 待写:假设 1、2、……(用 enumerate 环境,每条对应 04 里的式子/符号)
