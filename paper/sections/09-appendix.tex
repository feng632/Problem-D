% =====================================================================
%  09 附录
%  正文外的支撑内容:核心代码、补充数据、长表格等。
%  由 main.tex 的 appendices 环境包裹,放在正文最后。
%  代码用 lstlisting 环境(模板已加载 listings 宏包)。
% =====================================================================

\section{核心代码}

\begin{lstlisting}[language=python]
# 示例:核心算法代码(比赛时替换为实际代码)
def main():
    pass
\end{lstlisting}

% \section{补充数据}
% (补充表格、详细数据等)
